<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.2.7.RELEASE</version>
        <relativePath/>
    </parent>

	<groupId>com.isxcode.star</groupId>
	<artifactId>spark-star-parent</artifactId>
	<version>1.2.0</version>
	<name>spark-star</name>
	<description>spark-star</description>
	<packaging>pom</packaging>

	<modules>
        <module>star-api</module>
        <module>star-common</module>
        <module>star-client</module>
		<module>star-server</module>
        <module>star-plugins</module>
        <module>star-dist</module>
        <module>star-local-test/use-client-example</module>
    </modules>

    <properties>
        <java.version>1.8</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spark.version>3.1.1</spark.version>
        <scala.version>2.12</scala.version>
        <hadoop.version>3.2.2</hadoop.version>
        <jsoup.vesion>1.15.3</jsoup.vesion>
        <fastjson.version>2.0.20</fastjson.version>
        <lombok.version>1.18.24</lombok.version>
        <log4j.version>[2.16.0,)</log4j.version>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
            <version>${fastjson.version}</version>
        </dependency>

    </dependencies>

</project>
